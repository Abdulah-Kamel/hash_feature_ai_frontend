export const metadata = {
  title: "التحقق من الرمز",
  description: "أدخل رمز التحقق لإعادة تعيين كلمة السر",
  robots: {
    index: false,
    follow: true,
  },
};

export default function VerifyCodeLayout({ children }) {
  return children;
}
