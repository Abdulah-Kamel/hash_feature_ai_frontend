export const metadata = {
  title: "التحقق من الدفع",
  description: "جاري التحقق من حالة الدفع",
  robots: {
    index: false,
    follow: true,
  },
};

export default function SubscriptionValidationLayout({ children }) {
  return children;
}
